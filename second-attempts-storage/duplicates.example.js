/**
 * Card numbers that we know were actually successful.
 * @type { string[] }
 */
const already_found = [];

/**
 * @type { number }
 */
const start_from = 0;

module.exports = { already_found, start_from };
